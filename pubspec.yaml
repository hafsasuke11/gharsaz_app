name: gharsaz_app
description: A professional Pakistani home services app.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^4.0.0
  cloud_firestore: ^6.0.0

  shimmer: ^3.0.0

  firebase_auth: ^6.4.0

  # State Management
  provider: ^6.1.2

  intl: ^0.19.0

  # Fonts
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true