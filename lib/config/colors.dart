import 'package:flutter/material.dart';

class AppColors {
  // Main Colors
  static const Color primary = Color(0xFF14532D);

  static const Color secondary = Color(0xFF6EE7B7);

  static const Color accent = Color(0xFFFFC857);

  // Backgrounds
  static const Color background = Color(0xFFFFFBF7);

  static const Color softBackground = Color(0xFFF7F3EE);

  static const Color cardColor = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF1F2937);

  static const Color textSecondary = Color(0xFF6B7280);

  // Borders
  static const Color border = Color(0xFFE5E7EB);
}